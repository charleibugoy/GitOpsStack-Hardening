apiVersion: v1
kind: Service
metadata:
  name: nginx-hardened
  namespace: production
spec:
  type: NodePort
  selector:
    app: nginx-hardened
  ports:
  - port: 80
    targetPort: 80
    NodePort: 8080